# Server Port (default: 5001)
PORT=5001

# Privy App Credentials (for embedded server wallets)
PRIVY_APP_ID=YOUR_PRIVY_APP_ID
PRIVY_APP_SECRET=YOUR_PRIVY_APP_SECRET

# Agent Private Key (the registered agent wallet)
# If left blank, Vouch will dynamically generate a test agent wallet at startup
# and print the AgentBook CLI registration instructions.
AGENT_PRIVATE_KEY=0x...


# Aiven PostgreSQL (required — both dev and prod use the same DB)
DATABASE_URL=postgresql://user:password@host:port/defaultdb

# World ID Configuration
VITE_WORLD_APP_ID=app_staging_your_app_id
VITE_WORLD_ACTION=verify
WORLD_APP_ID=app_staging_your_app_id
WORLD_ACTION=verify
WORLD_RP_ID=rp_your_rp_id
WORLD_PRIVATE_KEY=0x...
